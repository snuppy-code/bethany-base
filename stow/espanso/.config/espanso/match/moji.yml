matches:
    - trigger: ",oi"
      replace: "{{emoji}}"
      vars:
          - name: emoji
            type: choice
            params:
                values:
                    - "❌"
                    - "✅"
                    - "👍️"
                    - "👎️"
                    - "🪇️"
